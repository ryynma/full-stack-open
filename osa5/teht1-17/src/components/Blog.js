/* Ei käytetty tässä vaiheessa.

import React from 'react'

const Blog = ({blog}) => (
  <div>
    <div><a href={blog.url}>{blog.url}</a></div>
    <div>
      {blog.likes} likes
      <button>Like</button>
    </div>
    <div>added by {blog.user.name}</div>
  </div>  
)

export default Blog
*/